# RCM Denial Prediction Space — Demo Stub

This is a placeholder MLflow model registered as `rcm-denial-prediction-space:1.0.0`
to back the IMDE executive demo. It does not contain real model weights; it
declares an MLflow signature only so the Studio "Models" tab renders correctly.

- **Base model**: microsoft/BiomedNLP-PubMedBERT-base-uncased-abstract
- **Training run**: run-denial-pubmedbert-v3
- **Owner**: ds-priya-shah
- **Demo scope**: imde-rcm-denial-demo
